import 'package:flutter/material.dart';

class FileBlockWidget extends StatelessWidget {
  const FileBlockWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
